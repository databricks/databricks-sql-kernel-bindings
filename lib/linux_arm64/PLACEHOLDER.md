# linux_arm64 kernel archive — NOT YET COMMITTED

The prebuilt kernel static archive for **linux_arm64** is not in this directory yet.
Produce it via the kernel repo build-c-abi-libs workflow (native runner),
download the `kernel-c-abi-linux_arm64` artifact, commit the archive here, then tag
`lib/linux_arm64/vX.Y.Z`.
