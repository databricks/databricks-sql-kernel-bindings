# windows_amd64 kernel archive — NOT YET COMMITTED

The prebuilt kernel static archive for **windows_amd64** is not in this directory yet.
Produce it via the kernel repo build-c-abi-libs workflow (native runner),
download the `kernel-c-abi-windows_amd64` artifact, commit the archive here, then tag
`lib/windows_amd64/vX.Y.Z`.
